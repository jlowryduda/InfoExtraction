1. Convert training file to mallet format:

bin/mallet import-file --input coref-trainset.gold --output train.mallet

2. Create a classifier trained on the training data:

bin/mallet train-classifier --input train.mallet --output-classifier maxent.classifier